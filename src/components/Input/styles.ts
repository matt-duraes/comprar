import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    borderColor: '#c3c5cb',
    borderWidth: 1,
    paddingHorizontal: 16,
    height: 48,
    width: '100%',
    borderRadius: 8,
  }
})